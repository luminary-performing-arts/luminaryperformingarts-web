import type { SocialConfig } from "@/src/types";

/**
 * Public social media profiles.
 *
 * Keep profiles disabled until their official URLs
 * have been confirmed.
 */
export const social = {
  profiles: [],
} satisfies SocialConfig;
